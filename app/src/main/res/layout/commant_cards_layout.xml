<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="#E6E6E9"
    android:orientation="vertical">

    <RelativeLayout
        android:id="@+id/rating_lay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@color/white"
        android:gravity="center"
        android:paddingBottom="30dp"

        android:paddingTop="30dp">

        <TextView
            android:id="@+id/comant"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textColor="@color/colorblack"
            android:textSize="30dp" />

        <RatingBar
            android:id="@+id/go_rating"
            style="@style/foodRatingBar"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:layout_below="@+id/comant"
            android:layout_centerHorizontal="true"
            android:isIndicator="true" />

        <!-- <RatingBar
             android:layout_width="wrap_content"
             android:layout_height="wrap_content"
             android:isIndicator="true"
             android:rating="3"
             android:layout_centerHorizontal="true"
             android:layout_below="@+id/comant"
             android:id="@+id/go_rating"
             android:paddingTop="5dp"
             android:paddingBottom="5dp"
             android:progressBackgroundTint="@color/textblack"
             android:progressTint="@color/starcolor"
             style="?android:attr/ratingBarStyleSmall"/>-->

        <TextView
            android:id="@+id/date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@+id/go_rating"
            android:gravity="center"
            android:textColor="#88888F"
            android:textSize="20dp" />

    </RelativeLayout>


</LinearLayout>